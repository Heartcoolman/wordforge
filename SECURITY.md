# Security Policy

## 支持的版本

| 版本 | 安全支持 |
|---|---|
| 最新 stable | ✅ |
| 最新 pre-release（beta） | 有限支持（仅严重漏洞） |
| 历史版本（< 最新 stable） | ❌ 不再支持 |

## 漏洞报告渠道

**不要**通过公开 Issue 报告安全漏洞。

请通过以下私密渠道之一报告：

1. **GitHub Security Advisory（首选）**：[提交私密漏洞报告](https://github.com/Heartcoolman/wordforge/security/advisories/new)
2. **邮件**：`wettererkoble606@gmail.com`，标题以 `[SECURITY]` 开头

报告内容请包含：
- 漏洞类型（如 SQL 注入 / 越权 / JWT 伪造等）
- 复现步骤或概念验证（PoC）代码
- 影响范围（哪些版本 / 功能）
- 建议的修复思路（可选）

## 响应时间线

| 阶段 | 目标时间 |
|---|---|
| 确认收到报告 | 48 小时内 |
| 初步评估与严重性分级 | 7 天内 |
| 修复并发布补丁 | 30 天内（严重漏洞）/ 90 天内（中低危） |
| 公开披露 | 补丁发布后，与报告者协商时机 |

若评估后认为不构成安全漏洞，会通过报告渠道告知原因。

## 披露政策

- **协调披露**（Coordinated Disclosure）：漏洞修复发布前，不接受公开披露。
- 报告者可以选择在补丁发布后，在 GitHub Security Advisory 中以真实姓名 / 账号接受公开致谢。
- 如报告者希望匿名，致谢条目填写 "Anonymous"。

## 范围内（In-Scope）

- 认证 / 授权绕过（JWT 伪造、越权访问）
- SQL 注入 / 数据泄露
- AMAS 接口的未授权访问或数据篡改
- 管理后台 XSS / CSRF
- 自更新链路的完整性绕过（tarball 替换 / 签名校验缺失）

## 范围外（Out-of-Scope）

- 依赖库的已知漏洞（且无可利用的攻击链）
- 需要物理访问服务器的攻击
- 社会工程学攻击
- 自部署实例中用户自行修改配置引入的风险
